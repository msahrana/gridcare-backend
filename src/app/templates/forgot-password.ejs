<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />

        <title>GridCare — Password Reset</title>

        <style>
            body {
                margin: 0;
                padding: 0;
                background-color: #eef2f6;
                font-family: Arial, Helvetica, sans-serif;
                color: #172033;
            }

            .wrapper {
                width: 100%;
                padding: 40px 16px;
                box-sizing: border-box;
            }

            .container {
                max-width: 560px;
                margin: 0 auto;
                background: #ffffff;
                border-radius: 14px;
                overflow: hidden;
                box-shadow: 0 10px 35px rgba(15, 23, 42, 0.1);
            }

            /* Header */
            .header {
                background: #0b1220;
                padding: 28px 35px;
                text-align: center;
                border-bottom: 4px solid #f59e0b;
            }

            .logo {
                font-size: 28px;
                font-weight: 800;
                letter-spacing: -0.5px;
                color: #ffffff;
                margin: 0;
            }

            .logo span {
                color: #f59e0b;
            }

            .tagline {
                margin: 7px 0 0;
                font-size: 12px;
                color: #94a3b8;
                letter-spacing: 1.5px;
                text-transform: uppercase;
            }

            /* Content */
            .content {
                padding: 40px 38px;
            }

            .icon {
                width: 52px;
                height: 52px;
                line-height: 52px;
                margin-bottom: 20px;
                text-align: center;
                border-radius: 12px;
                background: #fff7e6;
                color: #f59e0b;
                font-size: 24px;
                font-weight: bold;
            }

            .title {
                margin: 0 0 12px;
                font-size: 25px;
                line-height: 1.3;
                color: #111827;
            }

            .text {
                margin: 0 0 16px;
                font-size: 15px;
                line-height: 1.7;
                color: #64748b;
            }

            .name {
                color: #111827;
                font-weight: 700;
            }

            /* OTP */
            .otp-container {
                margin: 28px 0;
                padding: 25px;
                text-align: center;
                background: #f8fafc;
                border: 1px solid #e2e8f0;
                border-radius: 12px;
            }

            .otp-label {
                margin: 0 0 12px;
                font-size: 11px;
                font-weight: 700;
                color: #64748b;
                letter-spacing: 1.5px;
                text-transform: uppercase;
            }

            .otp {
                display: inline-block;
                padding: 13px 22px;
                background: #0b1220;
                color: #f59e0b;
                border-radius: 8px;
                font-size: 30px;
                line-height: 1;
                font-weight: 800;
                letter-spacing: 8px;
                font-family: 'Courier New', Courier, monospace;
            }

            .expiration {
                margin: 14px 0 0;
                font-size: 12px;
                color: #64748b;
            }

            .expiration strong {
                color: #334155;
            }

            /* Security Notice */
            .security {
                margin-top: 25px;
                padding: 16px 18px;
                background: #fffaf0;
                border-left: 4px solid #f59e0b;
                border-radius: 6px;
            }

            .security-title {
                margin: 0 0 6px;
                font-size: 13px;
                font-weight: 700;
                color: #92400e;
            }

            .security-text {
                margin: 0;
                font-size: 12px;
                line-height: 1.6;
                color: #78716c;
            }

            /* Footer */
            .footer {
                padding: 24px 35px;
                background: #f8fafc;
                border-top: 1px solid #e2e8f0;
                text-align: center;
            }

            .footer-brand {
                margin: 0 0 8px;
                font-size: 14px;
                font-weight: 700;
                color: #334155;
            }

            .footer-text {
                margin: 0;
                font-size: 11px;
                line-height: 1.6;
                color: #94a3b8;
            }

            .footer-accent {
                color: #f59e0b;
            }

            @media only screen and (max-width: 600px) {
                .wrapper {
                    padding: 20px 10px;
                }

                .header {
                    padding: 24px 20px;
                }

                .content {
                    padding: 30px 22px;
                }

                .footer {
                    padding: 22px 20px;
                }

                .title {
                    font-size: 22px;
                }

                .otp {
                    font-size: 25px;
                    letter-spacing: 6px;
                }
            }
        </style>
    </head>

    <body>
        <div class="wrapper">
            <div class="container">
                <!-- GridCare Header -->
                <div class="header">
                    <h1 class="logo">Grid<span>Care</span></h1>

                    <p class="tagline">Intelligent Industrial Operations</p>
                </div>

                <!-- Main Content -->
                <div class="content">
                    <div class="icon">🔐</div>

                    <h2 class="title">Password Reset Request</h2>

                    <p class="text">
                        Hello
                        <span class="name"><%= name %></span>,
                    </p>

                    <p class="text">
                        We received a request to reset the password associated
                        with your GridCare account. Use the verification code
                        below to continue.
                    </p>

                    <!-- OTP -->
                    <div class="otp-container">
                        <p class="otp-label">Your Verification Code</p>

                        <div class="otp"><%= otp %></div>

                        <p class="expiration">
                            This code expires in
                            <strong> <%= expirationMinutes %> minutes </strong>.
                        </p>
                    </div>

                    <!-- Security Notice -->
                    <div class="security">
                        <p class="security-title">Security Notice</p>

                        <p class="security-text">
                            If you did not request a password reset, you can
                            safely ignore this email. Never share this
                            verification code with anyone, including GridCare
                            support.
                        </p>
                    </div>
                </div>

                <!-- Footer -->
                <div class="footer">
                    <p class="footer-brand">
                        Grid<span class="footer-accent">Care</span>
                    </p>

                    <p class="footer-text">
                        Intelligent Industrial Operations Platform
                    </p>

                    <p class="footer-text">
                        &copy; <%= new Date().getFullYear() %> GridCare. All
                        rights reserved.
                    </p>
                </div>
            </div>
        </div>
    </body>
</html>
